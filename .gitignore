.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Secrets — never commit. Pre-commit hook also greps for rzp_ prefixes.
.env

# Ephemeral SQLite database files
*.db

# Data directory — ignore runtime databases but track golden payloads
data/*.db
data/*_scoring_log.jsonl
!data/golden/
!data/golden/**
